# MedVault Monorepo

This repository contains two top-level folders:
- `medvault/` — Spring Boot backend
- `medvault-frontend/` — React frontend

Run the backend from `medvault/` and the frontend from `medvault-frontend/`.

See each folder's README for specific run instructions.
